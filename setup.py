from setuptools import setup, find_packages
setup(name='MyStoreReact_BDD',
      version='1.0',
      description='BDD tests for MyStoreReact project',
      author='Alberto Tsang',
      packages=find_packages()
      )
